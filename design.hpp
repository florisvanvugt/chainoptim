using namespace std; // telling the compiler to use namespace "std",
		     // where the entire C++ library is declared.

class Design {

private:
  int ntrials;
  
public:   
  string print();
  Design(int ntrials);
};

